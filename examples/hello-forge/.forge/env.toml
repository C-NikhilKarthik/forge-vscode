# Tiny conda env for the Forge smoke test. Solves fast: just Python + pip, with
# huggingface_hub (provides the `hf` / `huggingface-cli` download command) via pip.

name = "forge-test"
channels = ["conda-forge"]
dependencies = ["python=3.11", "pip"]
pip = ["huggingface_hub"]
